if !exists('g:asyncRunCommand')
    let g:asyncRunCommand=''
endif

if !exists('g:makePanelCmd')
    let g:makePanelCmd='make'
endif

if !exists('g:makePanelTargetListLocation')
    let g:makePanelTargetListLocation='RemoteTargets.mk'
endif

if !exists('g:makePanelRootPath')
    let g:makePanelRootPath='~'
endif
